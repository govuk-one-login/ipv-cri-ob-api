name: Post merge

on:
  push:
    branches:
      - main

permissions: {}

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      # this can be condensed with a shared action
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: ./.github/actions/node-setup

      - name: Unit tests
        run: npm run test:coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage/
          retention-days: 1

  sonar-scan:
    name: Sonar scan
    runs-on: ubuntu-latest
    needs: unit-tests
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run Sonar scan
        uses: govuk-one-login/github-actions/code-quality/sonarcloud@9d6ee027d0b9167dfc25e67124951956278bb585
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          sonar-token: ${{ secrets.SONAR_TOKEN }}
          coverage-artifact: coverage
          coverage-run-id: ${{ github.run_id }} # || null?
          pull-repository: false

  # deploy-dev:
  #   uses: ./.github/workflows/_secure-pipeline-deploy.yml
  #   permissions:
  #     id-token: write
  #     contents: read
  #   secrets:
  #     role_arn: ${{ secrets.DEV_GH_ACTIONS_ROLE_ARN }}
  #     artifact_bucket: ${{ secrets.DEV_ARTIFACT_BUCKET }}
  #     container_sign_kms_key: ${{ secrets.DEV_CONTAINER_SIGN_KMS_KEY }}
  #     ecr_repository: ${{ secrets.DEV_ECR_REPOSITORY }}
  #     dynatrace_paas_token: ${{ secrets.DYNATRACE_PAAS_TOKEN }}

  #   deploy-build:
  #   uses: ./.github/workflows/_deploy.yml
  #   permissions:
  #     id-token: write
  #     contents: read
  #   secrets:
  #     role_arn: ${{ secrets.BUILD_GH_ACTIONS_ROLE_ARN }}
  #     artifact_bucket: ${{ secrets.BUILD_ARTIFACT_BUCKET }}
  #     container_sign_kms_key: ${{ secrets.BUILD_CONTAINER_SIGN_KMS_KEY }}
  #     ecr_repository: ${{ secrets.BUILD_ECR_REPOSITORY }}
  #     dynatrace_paas_token: ${{ secrets.DYNATRACE_PAAS_TOKEN }}

# Run some scans? NO now in run-scans?
# Why get unit test coverage - actually
# useful to have up to date main test coverage, and PRs can be merged when no conflicts I think?
# so not always up to date on PR coverage.
