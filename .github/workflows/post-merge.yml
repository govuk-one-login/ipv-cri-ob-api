# # name: Post merge

# # on:
# #   push:
# #     branches:
# #       - main

# # jobs:
# #   sonar-scan:
# #     runs-on: ubuntu-latest
# #     permissions:
# #       contents: read
# #     steps:
# #       - name: Checkout
# #         uses: actions/checkout@v6
# #         with:
# #           fetch-depth: 0 # required for sonar
# #       - name: Setup Node
# #         uses: ./.github/actions/node-setup
# #       - name: Unit tests
# #         run: npm run test:coverage
# #       - name: Sonar scan
# #         uses: SonarSource/sonarqube-scan-action@v7.0.0
# #         env:
# #           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

# name: Post merge

# on:
#   push:
#     branches:
#       - main

# # jobs:
# #   unit-tests:
# #     runs-on: ubuntu-latest
# #     permissions:
# #       contents: read
# #     steps:
# #       - name: Checkout
# #         uses: actions/checkout@v6
# #       - name: Setup Node
# #         uses: ./.github/actions/node-setup
# #       - name: Unit tests
# #         run: npm run test
# #       - name: Upload coverage report
# #         uses: actions/upload-artifact@v4
# #         with:
# #           name: coverage
# #           path: coverage/
# #           retention-days: 1

# # why run

#   sonar-scan:
#     runs-on: ubuntu-latest
#     needs: unit-tests
#     permissions:
#       contents: read
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v6
#       - uses: ./.github/actions/sonar-scan
#         with:
#           coverage-artifact: coverage
#           coverage-run-id: ${{ github.run_id }}
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           sonar-token: ${{ secrets.SONAR_TOKEN }}

#   deploy-dev:
#     uses: ./.github/workflows/_deploy.yml
#     permissions:
#       id-token: write
#       contents: read
#     secrets:
#       role_arn: ${{ secrets.DEV_GH_ACTIONS_ROLE_ARN }}
#       artifact_bucket: ${{ secrets.DEV_ARTIFACT_BUCKET }}
#       container_sign_kms_key: ${{ secrets.DEV_CONTAINER_SIGN_KMS_KEY }}
#       ecr_repository: ${{ secrets.DEV_ECR_REPOSITORY }}
#       dynatrace_paas_token: ${{ secrets.DYNATRACE_PAAS_TOKEN }}

#   deploy-build:
#     uses: ./.github/workflows/_deploy.yml
#     permissions:
#       id-token: write
#       contents: read
#     secrets:
#       role_arn: ${{ secrets.BUILD_GH_ACTIONS_ROLE_ARN }}
#       artifact_bucket: ${{ secrets.BUILD_ARTIFACT_BUCKET }}
#       container_sign_kms_key: ${{ secrets.BUILD_CONTAINER_SIGN_KMS_KEY }}
#       ecr_repository: ${{ secrets.BUILD_ECR_REPOSITORY }}
#       dynatrace_paas_token: ${{ secrets.DYNATRACE_PAAS_TOKEN }}

# #   sonar-scan:
# #     runs-on: ubuntu-latest
# #     permissions:
# #       contents: read
# #     steps:
# #       - name: Checkout
# #         uses: actions/checkout@v6
# #         with:
# #           fetch-depth: 0 # required for sonar
# #       - name: Setup Node
# #         uses: ./.github/actions/node-setup
# #       - name: Unit tests
# #         run: npm run test:coverage
# #       - name: Sonar scan
# #         uses: SonarSource/sonarqube-scan-action@v7.0.0
# #         env:
# #           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

# # Run some scans? NO now in run-scans?
# # Why bother with unit test coverage?

# # then deploy to dev and build.
# # anything else?
# # we can reuse deploy to dev
# # but make own build one
# # but comment that out for now so
