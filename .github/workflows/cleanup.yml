# need pre-merge and also manually deployed
# problem of rate limits though
# and delete failed state / rollback complete - only does it for stable stacks?

# name cleanup stacks?

# think I'm gonna combine cleaning up stale manually deployed and pre-merge stacks here, and also stale log groups.

# - get stale pre-merge
# - delete stale pre-merge
# - get stale manual stack
# - delete stale manual stack
# Are the above a shared action we can re-use with different params?
# - stale log group action
