name: Node project setup
description: Checkout and install dependencies

runs:
  using: composite
  steps:
    - name: Node setup
      uses: actions/setup-node@v6
      with:
        node-version-file: '.nvmrc'
        cache: 'npm'
        package-manager-cache: true # what does this do
    - name: Install dependencies
      run: npm ci
      shell: bash
